# Fluxo Visual: Worker e Tarefas

## 🔄 Fluxo Completo Passo a Passo

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. DEFINIÇÃO (Código Python)                                    │
└─────────────────────────────────────────────────────────────────┘

app/tasks/lawsuit_tasks.py:
┌─────────────────────────────────────────┐
│ @celery_app.task(...)                   │  ← Registra a função
│ def collect_lawsuit_task(...):          │     como uma tarefa
│     # código da tarefa                  │
└─────────────────────────────────────────┘
         │
         │ (registro)
         ▼
┌─────────────────────────────────────────┐
│ Celery Registry                         │
│ - "app.tasks.lawsuit_tasks.             │
│    collect_lawsuit_task" → função       │
└─────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│ 2. INICIALIZAÇÃO DO WORKER (Comando)                            │
└─────────────────────────────────────────────────────────────────┘

docker-compose.yml:
┌─────────────────────────────────────────┐
│ worker:                                 │
│   command: celery -A app.workers.       │
│            celery_app worker            │
└─────────────────────────────────────────┘
         │
         │ (executa)
         ▼
┌─────────────────────────────────────────┐
│ Processo Worker (Container)             │
│ ┌─────────────────────────────────────┐ │
│ │ 1. Importa celery_app               │ │
│ │ 2. Importa tasks (include)          │ │
│ │ 3. Registra todas as tarefas        │ │
│ │ 4. Conecta ao Redis                 │ │
│ │ 5. Loop:                            │ │
│ │    while True:                      │ │
│ │      mensagem = redis.get()         │ │
│ │      if mensagem:                   │ │
│ │        executar_tarefa(mensagem)    │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
         │
         │ (escutando)
         ▼
┌─────────────────────────────────────────┐
│ Redis (Fila)                            │
│ ┌─────────────────────────────────────┐ │
│ │ Fila: "celery"                      │ │
│ │ [vazia - esperando mensagens]       │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│ 3. REQUISIÇÃO HTTP (API)                                        │
└─────────────────────────────────────────────────────────────────┘

Cliente:
┌─────────────────────────────────────────┐
│ GET /lawsuit?lawsuit_number=CNJ        │
└─────────────────────────────────────────┘
         │
         ▼
app/controllers/lawsuit_controller.py:
┌─────────────────────────────────────────┐
│ cached = get_cached_cases(...)          │
│ if not cached:                          │
│     task = collect_lawsuit_task.        │
│              delay(cnj, 3600)  ←──────┐ │
│     return {"status": "queued"}        │ │
└─────────────────────────────────────────┘ │
         │                                  │
         │ (retorna imediatamente)         │
         ▼                                  │
┌─────────────────────────────────────────┐ │
│ Resposta: {"status": "queued"}          │ │
└─────────────────────────────────────────┘ │
                                            │
                                            │ (enfileira)
                                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. ENFILEIRAMENTO (Redis)                                       │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Redis (Fila)                            │
│ ┌─────────────────────────────────────┐ │
│ │ Fila: "celery"                      │ │
│ │ [                                    │ │
│ │   {                                  │ │
│ │     "task": "app.tasks.lawsuit_     │ │
│ │              tasks.collect_         │ │
│ │              lawsuit_task",         │ │
│ │     "args": ["CNJ", 3600],          │ │
│ │     "id": "abc123..."               │ │
│ │   }                                  │ │
│ │ ]                                    │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
         │
         │ (worker pega)
         ▼


┌─────────────────────────────────────────────────────────────────┐
│ 5. EXECUÇÃO (Worker)                                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Processo Worker                        │
│ ┌─────────────────────────────────────┐ │
│ │ 1. Pega mensagem do Redis           │ │
│ │ 2. Lê: task = "app.tasks...         │ │
│ │         collect_lawsuit_task"       │ │
│ │ 3. Busca função no registry         │ │
│ │ 4. Executa:                         │ │
│ │    collect_lawsuit_task(            │ │
│ │        "CNJ", 3600                  │ │
│ │    )                                │ │
│ │ 5. Chama get_lawsuit_data()         │ │
│ │    - Crawler                        │ │
│ │    - Scraper                        │ │
│ │    - MongoDB                        │ │
│ │ 6. Salva resultado no Redis         │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
         │
         │ (salva)
         ▼
┌─────────────────────────────────────────┐
│ MongoDB                                 │
│ ┌─────────────────────────────────────┐ │
│ │ cases, parties, movements           │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│ 6. CONSULTA (Segunda Requisição)                                │
└─────────────────────────────────────────────────────────────────┘

Cliente:
┌─────────────────────────────────────────┐
│ GET /lawsuit?lawsuit_number=CNJ        │
└─────────────────────────────────────────┘
         │
         ▼
app/controllers/lawsuit_controller.py:
┌─────────────────────────────────────────┐
│ cached = get_cached_cases(...)          │
│ if cached:  ←─── Agora tem cache!      │
│     return cached                       │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ Resposta: [{tribunal: "TJAL", ...}]    │
└─────────────────────────────────────────┘
```

---

## 🎯 Respostas Diretas

### 1. Onde o worker é definido?

**Resposta:** O worker **não é definido no código Python**. Ele é um **processo** iniciado pelo comando:

```bash
celery -A app.workers.celery_app worker
```

Este comando:
- Cria um processo Python
- Importa `app.workers.celery_app`
- Importa todas as tarefas (via `include`)
- Conecta ao Redis
- Fica em loop esperando mensagens

**Onde está no código:**
- **Definição da tarefa:** `app/tasks/lawsuit_tasks.py` (linha 9)
- **Configuração do Celery:** `app/workers/celery_app.py`
- **Comando do worker:** `docker-compose.yml` (linha 52)

### 2. Como a tarefa é atribuída ao worker?

**Resposta:** Não há "atribuição" manual. O processo é automático:

1. **API enfileira:** `task.delay()` → envia mensagem para Redis
2. **Worker pega:** Worker (que está escutando) vê a mensagem
3. **Worker executa:** Worker executa a função correspondente

É como um **sistema de fila**: quem está livre pega a próxima tarefa.

### 3. Por que container separado?

**Resposta:** Porque permite:

✅ **Escalabilidade:** Pode ter 1 API e 10 workers
✅ **Isolamento:** Se worker travar, API continua funcionando
✅ **Recursos:** Pode limitar CPU/memória separadamente
✅ **Deploy:** Pode atualizar workers sem reiniciar API
✅ **Monitoramento:** Logs e métricas separados

**Alternativa (mesmo container):**
```yaml
api:
  command: sh -c "uvicorn app.main:app & celery worker & wait"
```
Mas isso é **não recomendado** para produção.

---

## 🔍 Comandos Úteis para Entender

```bash
# Ver tarefas registradas no worker
docker exec data_collection_worker celery -A app.workers.celery_app inspect registered

# Ver tarefas sendo executadas agora
docker exec data_collection_worker celery -A app.workers.celery_app inspect active

# Ver estatísticas do worker
docker exec data_collection_worker celery -A app.workers.celery_app stats

# Ver fila no Redis
docker exec data_collection_redis redis-cli LLEN celery
docker exec data_collection_redis redis-cli LRANGE celery 0 -1
```

